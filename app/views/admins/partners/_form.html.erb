<%= simple_form_for [:admins, @partner] do |f| %>
	<div class="row">
		<div class="col-lg-8">
			<div class="card">
				<div class="card-body">
					<%= f.error_notification %>
					<fieldset>
						<%= f.input :image, as: :file, input_html: { class: "mb-3", accept: "image/gif, image/png, image/jpg, image/jpeg" } %>
						<%= f.input :name, as: :string %>
					</fieldset>
					<%= f.error :base %>
					<div class="form-actions" style="margin-top:100px;">
						<%= f.button :submit, :class => 'btn btn-secondary' %>
						<% unless @partner.new_record? %>
							<%= link_to "Cancel", admins_partner_path(@partner), :class => 'btn btn-light' %>
						<% else %>
							<%= link_to "Cancel", admins_partners_path, :class => 'btn btn-light' %>
						<% end %>
					</div>
				</div>
			</div>
		</div>
		<div class="col-lg-4">
			<div class="row">
				<%= render "admins/shared/error_messages", :target => @partner, col: 'col-lg-12' %>
			</div>
		</div>
	</div>
<% end %>
